apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: oracle-mcp-connections
  namespace: loki-toolhive-oracle-mcp
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi

